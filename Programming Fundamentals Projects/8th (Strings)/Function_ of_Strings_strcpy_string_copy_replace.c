#include <stdio.h>
#include <conio.h>
#include<string.h>
int main()
{ char name[45];
char name2[]="Abrar";
strcpy(name,name2); //strcpy(target,source)
printf("now name is %s ",name);
    

    return 0;
}