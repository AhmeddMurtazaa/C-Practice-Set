#include<stdio.h>
#include<conio.h>
main()
{
	int batch,fees;
		
		printf("write your batch:",batch);
		scanf("%d",&batch);
	    
	    
		if(batch==21)
	    {
		fees=21000;
		printf("dear student of batch %d your semester fees will be = %d",batch,fees);
        }
	    else
	    printf("error");
	    
		

}
