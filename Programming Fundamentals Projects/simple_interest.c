#include <stdio.h>
#include <conio.h>
int main()
{
    int principal=100,rate=1,years=10;
    int simpleInterest = (principal*rate*years)/100;
    printf("the value olf simple interest would be : %d ", simpleInterest);
    return 0;
}